import { Routes } from '@angular/router';
import { AccessibilityComponent } from './accessibility';

export const ACCESSIBILITY_ROUTES: Routes = [
  { path: '', component: AccessibilityComponent },
];
